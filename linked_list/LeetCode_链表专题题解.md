# LeetCode 链表专题题解

## 一、linked_list 目录 — 链表专题

本文把仓库里的高频链表题集中整理，方便按“快慢指针、反转、合并、环、随机指针”来复习。

| 题号 | 题目 | 难度 | 核心标签 |
|------|------|------|----------|
| 2 | 两数相加 | 中等 | 模拟、链表进位 |
| 19 | 删除链表的倒数第 N 个节点 | 中等 | 快慢指针、虚拟头节点 |
| 21 | 合并两个有序链表 | 简单 | 双指针、链表合并 |
| 23 | 合并 K 个升序链表 | 困难 | 分治 / 堆 |
| 24 | 两两交换链表中的节点 | 中等 | 指针重连、虚拟头节点 |
| 25 | K 个一组翻转链表 | 困难 | 分组反转 |
| 138 | 随机链表的复制 | 中等 | 哈希 / 链表拆分 |
| 141 | 环形链表 | 简单 | 快慢指针 |
| 142 | 环形链表 II | 中等 | Floyd 判环 |
| 148 | 排序链表 | 中等 | 归并排序 |
| 160 | 相交链表 | 简单 | 双指针 |
| 206 | 反转链表 | 简单 | 迭代 / 递归 |
| 234 | 回文链表 | 简单 | 快慢指针、反转后半段 |

---

## 二、第 19 题详解 — 删除链表的倒数第 N 个节点

### 题目描述
给你一个链表，删除链表的倒数第 `n` 个节点，并返回链表的头节点。

### 示例
- `head = [1,2,3,4,5], n = 2` -> `[1,2,3,5]`
- `head = [1], n = 1` -> `[]`

### 核心思路：快慢指针找到待删节点前驱

使用一个虚拟头节点 `dummy` 指向原链表头。

然后：
- 快指针先向前走 `n + 1` 步
- 再让快慢指针一起走
- 当快指针走到末尾时，慢指针正好停在待删除节点的前一个位置

这样删除时只需要：

```python
slow.next = slow.next.next
```

#### 代码
```python
class Solution:
    def removeNthFromEnd(self, head, n):
        dummy = ListNode(0, head)
        fast = slow = dummy

        for _ in range(n + 1):
            fast = fast.next

        while fast:
            fast = fast.next
            slow = slow.next

        slow.next = slow.next.next
        return dummy.next
```

#### 复杂度
- 时间复杂度：**O(n)**
- 空间复杂度：**O(1)**

---

## 三、链表题常见模板

### 1. 快慢指针
- `19`
- `141`
- `142`
- `234`

### 2. 链表反转
- `24`
- `206`
- `25`
- `234`

### 3. 链表合并
- `21`
- `23`

### 4. 特殊结构处理
- `138`
- `160`

---

## 四、建议复习顺序

1. `206` 反转链表
2. `21` 合并两个有序链表
3. `19` 删除链表的倒数第 N 个节点
4. `141` 环形链表
5. `142` 环形链表 II
6. `160` 相交链表
7. `234` 回文链表
8. `23` 合并 K 个升序链表
9. `24` 两两交换链表中的节点
10. `148` 排序链表
11. `25` K 个一组翻转链表
