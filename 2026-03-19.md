# 2026-03-19 LeetCode 热题 100 学习记录

## 一、binary_search 目录 — 二分查找专题

在 `binary_search/` 目录下创建了以下 LeetCode 热题 100 二分查找题目：

| 题号 | 题目 | 难度 | 文件 |
|------|------|------|------|
| 4 | 寻找两个正序数组的中位数 | 困难 | `4_median_of_two_sorted_arrays.py` |
| 33 | 搜索旋转排序数组 | 中等 | `33_search_in_rotated_sorted_array.py` |
| 34 | 在排序数组中查找元素的第一个和最后一个位置 | 中等 | `34_find_first_and_last_position.py` |
| 35 | 搜索插入位置 | 简单 | `35_search_insert_position.py` |
| 74 | 搜索二维矩阵 | 中等 | `74_search_a_2d_matrix.py` |
| 153 | 寻找旋转排序数组中的最小值 | 中等 | `153_find_minimum_in_rotated_sorted_array.py` |

---

## 二、第 4 题详解 — 寻找两个正序数组的中位数

### 题目描述
给定两个大小分别为 `m` 和 `n` 的正序（从小到大）数组 `nums1` 和 `nums2`，请你找出并返回这两个正序数组的**中位数**。
要求时间复杂度为 **O(log(m+n))**。

### 核心思路：二分分割

将问题转化为：**在两个有序数组中寻找第 k 小的数**。

#### 分割线思想
在 `nums1` 中找一个分割位置 `i`，在 `nums2` 中找一个分割位置 `j`，使得：
- 左半部分总共有 `(m + n + 1) // 2` 个元素
- `nums1[i-1] <= nums2[j]` 且 `nums2[j-1] <= nums1[i]`（交叉条件）

满足上述条件后，中位数由左半部分最大值和右半部分最小值决定。

#### 步骤
1. 对较短的数组做二分（保证 `m <= n`，在 `nums1` 上二分）
2. 二分范围：`[0, m]`，`i` 表示 `nums1` 左侧取几个元素
3. `j = half_len - i`，表示 `nums2` 左侧取几个元素
4. 比较边界值调整 `i`：
   - 若 `nums1[i-1] > nums2[j]`：`i` 太大，向左缩
   - 若 `nums2[j-1] > nums1[i]`：`i` 太小，向右扩
5. 找到合法分割后：
   - **总长度为奇数**：中位数 = 左半部分最大值
   - **总长度为偶数**：中位数 = (左半最大值 + 右半最小值) / 2

#### 代码
```python
def findMedianSortedArrays(nums1, nums2):
    if len(nums1) > len(nums2):
        nums1, nums2 = nums2, nums1
    m, n = len(nums1), len(nums2)
    half_len = (m + n + 1) // 2
    lo, hi = 0, m

    while lo <= hi:
        i = (lo + hi) // 2
        j = half_len - i

        if i < m and nums2[j-1] > nums1[i]:
            lo = i + 1
        elif i > 0 and nums1[i-1] > nums2[j]:
            hi = i - 1
        else:
            # 找到合法分割
            if i == 0:   max_left = nums2[j-1]
            elif j == 0: max_left = nums1[i-1]
            else:        max_left = max(nums1[i-1], nums2[j-1])

            if (m + n) % 2 == 1:
                return max_left

            if i == m:   min_right = nums2[j]
            elif j == n: min_right = nums1[i]
            else:        min_right = min(nums1[i], nums2[j])

            return (max_left + min_right) / 2.0
```

#### 复杂度
- 时间复杂度：**O(log(min(m,n)))**，只在较短数组上二分
- 空间复杂度：**O(1)**

---

## 三、linked_list 目录 — 链表专题

在 `linked_list/` 目录下创建了以下 LeetCode 热题 100 链表题目：

| 题号 | 题目 | 难度 | 文件 |
|------|------|------|------|
| 2 | 两数相加 | 中等 | `2_add_two_numbers.py` |
| 19 | 删除链表的倒数第 N 个节点 | 中等 | `19_remove_nth_node_from_end.py` |
| 21 | 合并两个有序链表 | 简单 | `21_merge_two_sorted_lists.py` |
| 23 | 合并 K 个升序链表 | 困难 | `23_merge_k_sorted_lists.py` |
| 25 | K 个一组翻转链表 | 困难 | `25_reverse_nodes_in_k_group.py` |
| 138 | 随机链表的复制 | 中等 | `138_copy_list_with_random_pointer.py` |
| 141 | 环形链表 | 简单 | `141_linked_list_cycle.py` |
| 142 | 环形链表 II | 中等 | `142_linked_list_cycle_ii.py` |
| 148 | 排序链表 | 中等 | `148_sort_list.py` |
| 160 | 相交链表 | 简单 | `160_intersection_of_two_linked_lists.py` |
| 206 | 反转链表 | 简单 | `206_reverse_linked_list.py` |
| 234 | 回文链表 | 简单 | `234_palindrome_linked_list.py` |

---

## 四、string 目录 — 字符串专题

在 `string/` 目录下创建了以下 LeetCode 热题 100 字符串题目：

| 题号 | 题目 | 难度 | 文件 |
|------|------|------|------|
| 3 | 无重复字符的最长子串 | 中等 | `3_longest_substring_without_repeating.py` |
| 5 | 最长回文子串 | 中等 | `5_longest_palindromic_substring.py` |
| 20 | 有效的括号 | 简单 | `20_valid_parentheses.py` |
| 32 | 最长有效括号 | 困难 | `32_longest_valid_parentheses.py` |
| 49 | 字母异位词分组 | 中等 | `49_group_anagrams.py` |
| 76 | 最小覆盖子串 | 困难 | `76_minimum_window_substring.py` |
| 131 | 分割回文串 | 中等 | `131_palindrome_partitioning.py` |
| 139 | 单词拆分 | 中等 | `139_word_break.py` |
| 208 | 实现 Trie（前缀树） | 中等 | `208_implement_trie.py` |
| 394 | 字符串解码 | 中等 | `394_decode_string.py` |
| 438 | 找到字符串中所有字母异位词 | 中等 | `438_find_all_anagrams.py` |

---

## 五、工程目录结构

```
codes/
├── binary_search/          # 二分查找专题（6题）
├── linked_list/            # 链表专题（12题）
├── string/                 # 字符串专题（11题）
├── Two_Pointers/           # 双指针专题
├── 2026-03-19.md           # 本学习记录
└── README.md
```
